SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (SID_NAME = CLRExtProc)
      (ORACLE_HOME = C:\\oracle\\xxx)
      (PROGRAM = extproc)
      (ENVS = "EXTPROC_DLLS=ONLY:C:\\oracle\\xxx\\bin\\oraxxx.dll")
    )
  )

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS =
        (PROTOCOL = TCP)
        (HOST = xxx.xxx.xxx.xxx)
        (PORT = xxx)
      )
    )
    (DESCRIPTION =
      (ADDRESS =
        (PROTOCOL = IPC)
        (KEY = EXTPROC1521)
      )
    )
  )

ADR_BASE_LISTENER = "C:\\Oracle"
LOGGING_LISTENER = OFF
DIAG_ADR_ENABLED_LISTENER = OFF